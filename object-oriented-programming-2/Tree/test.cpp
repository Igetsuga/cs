#include <iostream>
#include <utility>
#include <math.h>
#include <tuple>
#include <set>

int main() {	

	int a = 0; std::cin >> a;
	int b = 0; std::cin >> b;
	int c = 0; std::cin >> c;

	int x = 0; std::cin >> x;
	int y = 0; std::cin >> y;
	int z = 0; std::cin >> z;

	int sum = (a * x) + (y * b) + (z * c);
	
	std::tuple<int, int, int> tuple;
	std::set<std::tuple<int,int,int>> set_tuple;	

	int aa {2.4};
	

	// std::cout << 


}